# Legacy runtime API used by the Vite client.
VITE_RUNTIME_API_URL=http://localhost:8000

# Assurance Workbench v1 is deliberately opt-in on both sides.
# The server flag authorizes/routes the API; the Vite flag only renders UI.
VITE_ASSURANCE_V1=false
REAGENT_ASSURANCE_V1=false
