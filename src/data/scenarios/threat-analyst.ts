import type { Scenario } from "@/types/simulator";

export const threatAnalyst: Scenario = {
  id: "threat-analyst",
  title: "The Threat Analyst",
  brief: "Fix a bleeding-money SOC pipeline: 5 single-tool MCPs, all Opus, no eval, no human review — and it just auto-published a false positive.",
  description:
    "You've inherited a Security Operations Center's automated threat intelligence system. The previous engineer built it with 5 separate MCP servers (one per tool), every executor runs Claude Opus, there's no quality gate, no error handling on the threat feed parser, and critical alerts go straight to output without human review. Last week it auto-published an unreviewed 'CRITICAL' threat brief that turned out to be a false positive. Leadership wants it fixed yesterday. Your job: consolidate MCPs by domain, right-size models, add evaluation loops with context hygiene, handle unreliable threat feeds, gate critical alerts through human review, and dispatch alerts via Kafka stream.",
  mode: "fixer",
  difficulty: "hard",
  expectedInputs: "Raw threat indicators: IP addresses, domain names, file hashes, CVE references, monitoring alerts",
  expectedOutputs: "Structured threat intelligence brief with severity classification, IOC analysis, recommended actions, and confidence assessment",
  availableNodeTypes: [
    "input", "output", "executor", "evaluator", "router",
    "context_gate", "web_search", "file_rw", "tool_rag",
    "fallback_router", "code_exec", "api_call", "human_review",
    "mcp_server", "kafka_stream",
  ],
  initialNodes: [
    { id: "input-1", type: "input", config: { label: "Threat Indicators" }, position: { x: 50, y: 300 }, locked: true },
    {
      id: "mcp-osint", type: "mcp_server",
      config: { label: "OSINT MCP", servedTools: ["web_search"] },
      position: { x: 220, y: 140 },
    },
    {
      id: "mcp-feed", type: "mcp_server",
      config: { label: "Feed MCP", servedTools: ["file_rw"] },
      position: { x: 220, y: 300 },
    },
    {
      id: "mcp-intel", type: "mcp_server",
      config: { label: "Intel MCP", servedTools: ["tool_rag"] },
      position: { x: 220, y: 460 },
    },
    {
      id: "exec-enricher", type: "executor",
      config: {
        label: "Threat Enricher",
        model: "claude-opus",
        systemPrompt: "",
      },
      position: { x: 450, y: 140 },
    },
    {
      id: "exec-correlator", type: "executor",
      config: {
        label: "IOC Correlator",
        model: "claude-opus",
        systemPrompt: "",
      },
      position: { x: 450, y: 300 },
    },
    {
      id: "mcp-sandbox", type: "mcp_server",
      config: { label: "Sandbox MCP", servedTools: ["code_exec"] },
      position: { x: 450, y: 460 },
    },
    {
      id: "exec-analyst", type: "executor",
      config: {
        label: "Threat Analyst",
        model: "claude-opus",
        systemPrompt: "",
      },
      position: { x: 680, y: 300 },
    },
    {
      id: "mcp-alert", type: "mcp_server",
      config: { label: "Alert MCP", servedTools: ["api_call"] },
      position: { x: 680, y: 460 },
    },
    {
      id: "exec-reporter", type: "executor",
      config: {
        label: "Brief Writer",
        model: "claude-opus",
        systemPrompt: "",
      },
      position: { x: 900, y: 300 },
    },
    { id: "output-1", type: "output", config: { label: "Threat Brief" }, position: { x: 1120, y: 300 }, locked: true },
  ],
  initialEdges: [
    { id: "e1", source: "input-1", target: "mcp-osint" },
    { id: "e2", source: "input-1", target: "mcp-feed" },
    { id: "e3", source: "input-1", target: "mcp-intel" },
    { id: "e4", source: "mcp-osint", target: "exec-enricher" },
    { id: "e5", source: "mcp-feed", target: "exec-correlator" },
    { id: "e6", source: "mcp-intel", target: "exec-correlator" },
    { id: "e7", source: "exec-enricher", target: "exec-analyst" },
    { id: "e8", source: "exec-correlator", target: "mcp-sandbox" },
    { id: "e9", source: "mcp-sandbox", target: "exec-analyst" },
    { id: "e10", source: "exec-analyst", target: "mcp-alert" },
    { id: "e11", source: "mcp-alert", target: "exec-reporter" },
    { id: "e12", source: "exec-reporter", target: "output-1" },
  ],
  hints: [
    "5 MCP servers with 1 tool each is worse than having tools directly — you're paying coordination overhead for zero benefit.",
    "Claude Opus on every executor is overkill. A threat enricher and severity router are classification tasks — tiny models handle them fine.",
    "The threat feed (File R/W) is unreliable. Without a Fallback Router, corrupted feed data poisons the entire pipeline.",
    "There's no Evaluator. The brief goes straight to output without quality checks — that's how a false positive got published.",
    "Critical threats should never auto-publish. Add Human Review before the output on the critical path.",
    "Raw OSINT data, feed results, and RAG lookups are noisy. A Context Gate before analysis filters out the noise.",
    "If the evaluator rejects the brief for revision, a SECOND Context Gate prevents the old draft from bloating the revision loop.",
    "A Kafka Stream node publishes alerts asynchronously — faster than blocking on API calls.",
  ],
  failureSequence: {
    nodeType: "file_rw",
    pattern: [true, false, true],
    failureMessage: "Threat feed returned corrupted/partial indicator data",
  },
  maxCost: 12.0,
  maxLatency: 45.0,
  minReliability: 85,
  llmThresholds: { minPromptScore: 60, minArchitectureScore: 65 },
  editorial: {
    explanation:
      "Consolidate 5 single-tool MCPs into 1 Intel MCP (web_search, tool_rag, api_call → 3 tools = bonus). Keep file_rw and code_exec as standalone tools. Handle the unreliable threat feed with a Fallback Router + Gap Noter. Merge all data through a Context Gate (structured sendoff) to filter noise. Route by severity (Critical/Standard) with a lightweight model. Critical briefs get a capable model + output schema + Human Review. Both paths feed an Evaluator; fail path loops through a Revision Context Gate. Dispatch alerts via Kafka Stream. Every node serves the narrative — nothing is forced.",
    commonMistakes: [
      { mistake: "Keeping 5 single-tool MCPs", whyItFails: "Each MCP adds $0.30 coordination overhead. With 1 tool each, you pay the overhead for zero benefit — 5 × penalty" },
      { mistake: "Keeping Claude Opus on all executors", whyItFails: "Classification and gap-noting are simple tasks. Opus costs $15/1k tokens for work a $0.15 model handles perfectly" },
      { mistake: "No fallback on the threat feed", whyItFails: "When feed data is corrupted, the entire pipeline produces a garbage brief. Handle errors at the source, not at the end" },
      { mistake: "No evaluator or human review", whyItFails: "A false positive CRITICAL alert already shipped. You need quality gates AND human sign-off on high-stakes alerts" },
      { mistake: "No context gates", whyItFails: "Raw OSINT data (full HTML, noisy feeds, RAG chunks) overwhelms the analyst. And without loop hygiene, each revision bloats the prompt" },
    ],
    optimalCode: [
      "# Threat Analyst: Parallel intel → Error handling → Context filter → Triage → Quality loop → Human review → Alert dispatch",
      "graph = StateGraph()",
      "",
      "# Parallel intelligence gathering",
      "graph.add_node('intel_mcp', MCP(tools=[web_search, tool_rag, api_call]))",
      "graph.add_node('threat_feed', file_rw)  # Unreliable!",
      "graph.add_node('ioc_sandbox', code_exec)",
      "",
      "# Error handling at the source",
      "graph.add_node('feed_check', FallbackRouter())",
      "graph.add_node('gap_noter', Executor(model='gpt-4o-mini'))",
      "",
      "# Context management + Triage",
      "graph.add_node('intel_filter', ContextGate('structured_sendoff'))",
      "graph.add_node('severity_router', Router(routes=['Critical','Standard']))",
      "",
      "# Analysis + Quality loop",
      "graph.add_node('critical_analyst', Executor(model='gpt-4o', schema=THREAT_BRIEF))",
      "graph.add_node('standard_analyst', Executor(model='gpt-4o-mini'))",
      "graph.add_node('quality_check', Evaluator(model='gpt-4o-mini'))",
      "graph.add_node('revision_gate', ContextGate('structured_sendoff'))",
      "",
      "# Human gate + Alert dispatch",
      "graph.add_node('analyst_review', HumanReview('approval'))",
      "graph.add_node('alert_dispatch', KafkaStream())",
    ].join("\n"),
    keyConcepts: [
      "MCP consolidation: group by domain (3+ tools = bonus), don't wrap individual tools",
      "Model right-sizing: classification/gap-noting = mini models, analysis = medium models",
      "Error handling at the source: Fallback Router on unreliable feeds, Gap Noter flags what's missing",
      "Two-stage context management: Gate #1 filters noisy intel, Gate #2 manages revision loop hygiene",
      "Human-in-the-loop: Critical threats MUST have analyst sign-off before publication",
      "Structural guarantees: output schema enforces threat brief format at zero cost",
      "Kafka streaming: async alert dispatch is faster than blocking API calls",
    ],
  },
};
